#pragma once

class CgiSocket : public ASocket
{
	// int fd[2]
	// Response _response;

	// createSocket();
	// handleEpollInEvent();
	// handleEpollOutEvent(int epoll_fd, std::map<int, ASocket*>& _socket);
};